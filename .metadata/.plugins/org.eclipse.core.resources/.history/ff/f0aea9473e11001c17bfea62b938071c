package ch03;

import java.awt.Color;

import javax.swing.JPanel;

public class Activity2 extends BaseActivity implements CallbackCheckPosition {

	public Activity2(String name) {
		this.name = name;
		initData();
		setInitLayout();
		addEventListener();
		
	}
	
	@Override
	public void initData() {
		setTitle(name);
		setSize(500, 500);
		setDefaultCloseOperation(EXIT_ON_CLOSE);
		panel = new JPanel();
		
	}
	
	@Override
	public void setInitLayout() {
		setVisible(true);
		add(panel);
		panel.setBackground(Color.yellow);
	}
	
	@Override
	public void addEventListener() {
	}
	
}
